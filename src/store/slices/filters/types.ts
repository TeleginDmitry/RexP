export type FiltersState = {
  indexPage: {
    activeFilter: string;
  };
  myOrdersPage: {
    activeFilter: string;
  },
  sizes: {
    activeFilter: string;
  }
  productsInBasket: {
    activeFilter: string;
  }
  deliveryDetailsPage: {
    activeFilter: string;
  }
};
